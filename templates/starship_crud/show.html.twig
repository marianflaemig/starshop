{% extends 'base.html.twig' %}

{% block title %}Starship{% endblock %}

{% block body %}
    <h1>Starship</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ starship.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ starship.name }}</td>
            </tr>
            <tr>
                <th>Class</th>
                <td>{{ starship.class }}</td>
            </tr>
            <tr>
                <th>Captain</th>
                <td>{{ starship.captain }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ starship.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_starship_crud_index') }}">back to list</a>

    <a href="{{ path('app_starship_crud_edit', {'id': starship.id}) }}">edit</a>

    {{ include('starship_crud/_delete_form.html.twig') }}
{% endblock %}
