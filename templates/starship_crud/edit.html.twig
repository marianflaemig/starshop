{% extends 'base.html.twig' %}

{% block title %}Edit Starship{% endblock %}

{% block body %}
    <div class="flex flex-col items-center">
        <h1 class="text-4xl font-semibold mb-8 mt-10">Edit Starship</h1>
        {{ include('starship_crud/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="block w-[200px] mt-3 rounded-[60px] py-2 px-5 bg-white/10 hover:bg-white/20" href="{{ path('app_starship_crud_index') }}">Back to list</a>

        {{ include('starship_crud/_delete_form.html.twig') }}
    </div>
{% endblock %}
